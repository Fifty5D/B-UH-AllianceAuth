schema_version = 1
app = "platform"
kind = "fix"
summary = "Distinguish fatal log severity from ESI header/logger metadata without exempting DEBUG failures or truncating retained logs, rehearse immutable v0.6.2 recovery, and prepare a separately approved classifier-file continuation from the installed PR 62 receiver with all receipts, backups and the release hold preserved."
