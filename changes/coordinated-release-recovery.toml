schema_version = 1
app = "platform"
kind = "fix"
summary = "Restore the guarded release path with self-contained receiver tests, complete lineage checkout, exact owner diagnostics, root-private staging, and umask-stable permission coverage."
deployment_predecessor = "0.5.6"
