schema_version = 1
app = "platform"
kind = "fix"
summary = "Restore the guarded release path with self-contained receiver tests, complete lineage checkout, and exact MainProcess or Alliance Auth owner diagnostics."
deployment_predecessor = "0.5.6"
