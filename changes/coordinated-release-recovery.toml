schema_version = 1
app = "platform"
kind = "fix"
summary = "Restore the exact guarded release path with self-contained receiver tests, complete lineage checkout, and container-bound recovery diagnostics."
deployment_predecessor = "0.5.6"
